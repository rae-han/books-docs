# Chapter 20: Business Rules (업무 규칙)

## 핵심 질문

업무 규칙이란 정확히 무엇이며, "핵심 업무 규칙"과 "애플리케이션 특화 업무 규칙"은 어떻게 다른가? 엔티티(Entity)와 유스케이스(Use Case)는 각각 어떤 역할을 하며, 왜 엔티티가 유스케이스보다 고수준인가? 요청 및 응답 모델은 왜 독립적이어야 하는가?

---

## 1. 업무 규칙이란 무엇인가

애플리케이션을 업무 규칙과 플러그인으로 구분하려면, 업무 규칙이 실제로 무엇인지를 잘 이해해야 한다.

### 1.1 엄밀한 정의

업무 규칙은 **사업적으로 수익을 얻거나 비용을 줄일 수 있는 규칙 또는 절차**다. 더 엄밀하게 말하면, **컴퓨터상으로 구현했는지와 상관없이** 수익을 얻거나 비용을 줄일 수 있어야 한다. 심지어 사람이 수동으로 직접 수행하더라도 마찬가지다.

**예시**: "대출에 N%의 이자를 부과한다"는 사실은 은행이 돈을 버는 업무 규칙이다. 컴퓨터 프로그램으로 이자를 계산하든, 직원이 주판을 튕겨 계산하든 상관없다.

---

## 2. 핵심 업무 규칙과 핵심 업무 데이터

### 2.1 핵심 업무 규칙 (Critical Business Rule)

사업 자체에 핵심적이며, **규칙을 자동화하는 시스템이 없더라도 업무 규칙은 그대로 존재**하는 규칙이다.

### 2.2 핵심 업무 데이터 (Critical Business Data)

핵심 업무 규칙은 보통 데이터를 요구한다. 이러한 데이터는 **시스템으로 자동화되지 않은 경우에도 존재하는 그런 데이터**다.

**예시** — 대출(Loan)의 경우:

| 핵심 업무 데이터 | 설명 |
|----------------|------|
| 대출 잔액 (principle) | 대출한 원금의 잔액 |
| 이자율 (rate) | 적용되는 이자율 |
| 지급 일정 (period) | 상환 기간 |

핵심 규칙과 핵심 데이터는 본질적으로 결합되어 있기 때문에 **객체로 만들 좋은 후보**가 된다. 이러한 유형의 객체를 **엔티티(Entity)**(*이바 야콥슨(Ivar Jacobson)이 《Object Oriented Software Engineering》(1992)에서 이 개념에 붙인 이름이다.*)라고 한다.

---

## 3. 엔티티 (Entity)

### 3.1 정의

엔티티는 컴퓨터 시스템 내부의 객체로서, 핵심 업무 데이터를 기반으로 동작하는 일련의 조그만 **핵심 업무 규칙을 구체화**한다.

- 핵심 업무 데이터를 직접 포함하거나 매우 쉽게 접근할 수 있다
- 인터페이스는 핵심 업무 데이터를 기반으로 동작하는 핵심 업무 규칙을 구현한 함수들로 구성된다

### 3.2 Loan 엔티티 예제

```
┌────────────────────────┐
│         Loan           │
├────────────────────────┤
│ - principle            │
│ - rate                 │
│ - period               │
├────────────────────────┤
│ + makePayment()        │
│ + applyInterest()      │
│ + chargeLateFee()      │
└────────────────────────┘
```

| 요소 | 유형 | 설명 |
|------|------|------|
| principle, rate, period | 핵심 업무 데이터 | 대출과 관련된 핵심 정보 |
| makePayment() | 핵심 업무 규칙 | 대출 상환 처리 |
| applyInterest() | 핵심 업무 규칙 | 이자 적용 |
| chargeLateFee() | 핵심 업무 규칙 | 연체료 부과 |

### 3.3 엔티티의 독립성

이러한 종류의 클래스를 생성할 때, 업무에서 핵심적인 개념을 구현하는 소프트웨어는 한데 모으고, 구축 중인 자동화 시스템의 나머지 모든 고려사항과 **분리**시킨다.

엔티티는 **절대로** 다음에 의해 오염되어서는 안 된다:
- 데이터베이스
- 사용자 인터페이스
- 서드파티 프레임워크

> **핵심 통찰**: 엔티티는 순전히 업무에 대한 것이며, 이외의 것은 없다. 이 클래스는 어떤 시스템에서도 업무를 수행할 수 있으며, 시스템의 표현 형식이나 데이터 저장 방식, 해당 시스템에서 컴퓨터가 배치되는 방식과도 무관하다.

### 3.4 반드시 클래스일 필요는 없다

엔티티를 만드는 데 꼭 객체 지향 언어를 사용할 필요는 없다. 유일한 요구조건은 **핵심 업무 데이터와 핵심 업무 규칙을 하나로 묶어서 별도의 소프트웨어 모듈로 만들어야 한다는 것**이다.

---

## 4. 유스케이스 (Use Case)

### 4.1 정의

모든 업무 규칙이 엔티티처럼 순수한 것은 아니다. **자동화된 시스템이 동작하는 방법을 정의하고 제약**함으로써 수익을 얻거나 비용을 줄이는 업무 규칙도 존재한다. 이러한 규칙은 자동화된 시스템의 요소로 존재해야만 의미가 있으므로 수동 환경에서는 사용될 수 없다.

유스케이스(*엔티티와 마찬가지로 이바 야콥슨의 《Object Oriented Software Engineering》에서 소개된 개념이다.*)는 자동화된 시스템이 사용되는 방법을 설명한다:
- 사용자가 제공해야 하는 **입력**
- 사용자에게 보여줄 **출력**
- 해당 출력을 생성하기 위한 **처리 단계**

### 4.2 유스케이스 예제: 신규 대출을 위한 신상정보 수집

```
┌─────────────────────────────────────────────────────┐
│  유스케이스: 신규 대출을 위한 신상정보 수집하기        │
├─────────────────────────────────────────────────────┤
│  입력: 이름, 주소, 생일, 운전면허 번호, 주민번호 등    │
│  출력: 사용자 확인을 위한 동일한 정보 + 신용도         │
├─────────────────────────────────────────────────────┤
│  기본 과정:                                          │
│  1. 이름을 받아서 검증한다.                           │
│  2. 주소, 생일, 운전면허 번호, 주민번호 등을 검증한다. │
│  3. 신용도를 얻는다.                                 │
│  4. 신용도가 500보다 낮으면, Denial(거절)을 활성화.    │
│  5. 높으면 Customer를 생성하고                       │
│     Loan Estimation(대출 견적)을 활성화한다.          │
└─────────────────────────────────────────────────────┘
```

마지막 줄에서 **Customer를 언급**한다는 점에 주목하자. 이는 Customer 엔티티에 대한 참조이며, 은행과 고객의 관계를 결정짓는 핵심 업무 규칙은 바로 이 Customer 엔티티에 포함된다.

### 4.3 유스케이스의 역할

유스케이스는 엔티티 내부의 핵심 업무 규칙을 **어떻게, 그리고 언제 호출할지**를 명시하는 규칙을 담는다. **엔티티가 어떻게 춤을 출지를 유스케이스가 제어하는 것이다.**

### 4.4 유스케이스와 UI의 관계

유스케이스는 사용자 인터페이스를 **기술하지 않는다.** 인터페이스로 들어오는 데이터와 되돌려주는 데이터를 형식 없이 명시한다는 점만 빼면, 유스케이스만 봐서는 이 애플리케이션이 웹인지, 리치 클라이언트인지, 콘솔 기반인지, 순수한 서비스인지를 **구분하기 불가능**하다.

유스케이스는 시스템이 사용자에게 어떻게 보이는지를 설명하지 않는다. **애플리케이션에 특화된 규칙을 설명하며, 이를 통해 사용자와 엔티티 사이의 상호작용을 규정한다.** 시스템에서 데이터가 들어오고 나가는 방식은 유스케이스와는 무관하다.

---

## 5. 엔티티 vs 유스케이스: 수준의 차이

### 5.1 의존성 방향

**엔티티는 자신을 제어하는 유스케이스에 대해 아무것도 알지 못한다.** 이는 의존성 역전 원칙을 준수하는 의존성 방향에 대한 또 다른 예다.

```
의존성 방향:

    ┌──────────────┐
    │   엔티티     │  ← 고수준 (아무것도 모름)
    │  (Entity)    │
    └──────────────┘
           △
           │ (의존)
    ┌──────────────┐
    │  유스케이스   │  ← 저수준 (엔티티를 알고 있음)
    │ (Use Case)   │
    └──────────────┘
```

### 5.2 왜 엔티티가 고수준인가?

| 항목 | 엔티티 | 유스케이스 |
|------|--------|-----------|
| 범용성 | 수많은 다양한 애플리케이션에서 사용 가능하도록 **일반화** | 단일 애플리케이션에 **특화** |
| 입출력과의 거리 | 각 시스템의 입력/출력에서 **더 멀리** 떨어져 있음 | 해당 시스템의 입출력에 **보다 가깝게** 위치 |
| 의존성 | 유스케이스에 **의존하지 않는다** | 엔티티에 **의존한다** |

유스케이스는 단일 애플리케이션에 특화되어 있으며, 해당 시스템의 입력과 출력에 보다 가깝게 위치하기 때문이다. 엔티티는 수많은 다양한 애플리케이션에서 사용될 수 있도록 일반화된 것이므로, 각 시스템의 입력이나 출력에서 더 멀리 떨어져 있다.

---

## 6. 요청 및 응답 모델

### 6.1 독립성의 원칙

유스케이스는 입력 데이터를 받아서 출력 데이터를 생성한다. 제대로 구성된 유스케이스 객체라면 데이터를 사용자나 또 다른 컴포넌트와 주고받는 방식에 대해서는 **전혀 눈치챌 수 없어야** 한다.

유스케이스 클래스의 코드가 HTML이나 SQL에 대해 알게 되는 일을 **절대로** 원치 않는다.

### 6.2 단순한 데이터 구조

유스케이스는 **단순한 요청 데이터 구조**를 입력으로 받아들이고, **단순한 응답 데이터 구조**를 출력으로 반환한다:

| 특성 | 설명 |
|------|------|
| 의존성 없음 | 이들 데이터 구조는 어떤 것에도 의존하지 않는다 |
| 프레임워크와 무관 | HttpRequest나 HttpResponse 같은 표준 프레임워크 인터페이스로부터 파생되지 않는다 |
| UI와 무관 | 웹뿐만 아니라 그 어떤 사용자 인터페이스에도 종속되지 않는다 |

### 6.3 엔티티 참조의 유혹

엔티티 객체를 가리키는 참조를 요청 및 응답 데이터 구조에 포함하려는 **유혹**을 받을 수 있다. 엔티티와 요청/응답 모델은 상당히 많은 데이터를 공유하므로 적합해 보일 수도 있다.

**하지만 이 유혹을 떨쳐내라!** 이들 두 객체의 목적은 완전히 다르다:

| 항목 | 엔티티 | 요청/응답 모델 |
|------|--------|---------------|
| 목적 | 핵심 업무 규칙 구체화 | 유스케이스의 입출력 데이터 전달 |
| 변경 이유 | 업무 규칙 변경 | 유스케이스 요구사항 변경 |
| 함께 묶으면? | 공통 폐쇄 원칙(CCP)과 단일 책임 원칙(SRP) 위배 |

두 객체를 어떤 식으로든 함께 묶으면, 코드에는 수많은 **떠돌이 데이터(tramp data)**(*tramp data — 해당 데이터를 실제로 사용할 함수로 전달하기 위해 함수에서 함수로 전달되는 데이터. 전달 경로의 앞쪽 함수에서는 해당 데이터를 전혀 사용하지 않는다.*)가 만들어지고, 수많은 조건문이 추가되어 버린다.

---

## 7. 업무 규칙의 위상

업무 규칙은 소프트웨어 시스템이 존재하는 이유다. 업무 규칙은 **핵심적인 기능**이다. 업무 규칙은 수익을 내고 비용을 줄이는 코드를 수반한다. 업무 규칙은 **집안의 가보**다.

> **핵심 통찰**: 업무 규칙은 사용자 인터페이스나 데이터베이스와 같은 저수준의 관심사로 인해 오염되어서는 안 되며, 원래 그대로의 모습으로 남아 있어야 한다. 업무 규칙을 표현하는 코드는 반드시 시스템의 심장부에 위치해야 하며, 덜 중요한 코드는 이 심장부에 플러그인되어야 한다. 업무 규칙은 시스템에서 **가장 독립적이며 가장 많이 재사용할 수 있는 코드**여야 한다.

---

## 요약

- **업무 규칙**은 사업적으로 수익을 얻거나 비용을 줄일 수 있는 규칙 또는 절차다
- **핵심 업무 규칙**은 컴퓨터 없이도 존재하는 규칙이며, **핵심 업무 데이터**와 함께 **엔티티**로 캡슐화된다
- **유스케이스**는 자동화된 시스템에서만 의미 있는 **애플리케이션 특화** 업무 규칙이다
- 엔티티는 유스케이스보다 **고수준**이다 — 더 범용적이며, 입출력에서 더 멀리 떨어져 있다
- **유스케이스는 엔티티에 의존하지만, 엔티티는 유스케이스에 의존하지 않는다**
- 요청 및 응답 모델은 엔티티와 **독립적**이어야 한다 — 프레임워크나 UI에 종속되어서는 안 된다
- 업무 규칙은 시스템의 **심장부**에 위치해야 하며, 나머지 모든 것은 플러그인이다

---

## 다른 챕터와의 관계

- **Chapter 15 (아키텍처란?)**: "정책 vs 세부사항" 구분에서 업무 규칙은 가장 핵심적인 정책에 해당한다
- **Chapter 19 (정책과 수준)**: 엔티티가 유스케이스보다 고수준이라는 관계가 19장의 "수준" 개념을 구체적으로 보여준다
- **Chapter 11 (DIP)**: 엔티티가 유스케이스에 의존하지 않는 구조는 의존성 역전 원칙의 직접적인 적용이다
- **Chapter 7 (SRP), Chapter 13 (CCP)**: 요청/응답 모델을 엔티티와 분리해야 하는 근거가 SRP와 CCP에 있다
- **Chapter 17 (경계: 선 긋기)**: 업무 규칙과 데이터베이스/UI 사이에 경계선을 긋는 것이 이 장의 엔티티·유스케이스 분리와 일맥상통한다
- **Chapter 21 (소리치는 아키텍처)**: 유스케이스가 아키텍처의 중심이어야 한다는 주장이 21장에서 "아키텍처는 유스케이스에 대해 소리쳐야 한다"는 관점으로 발전한다
- **Chapter 22 (클린 아키텍처)**: 이 장에서 정의한 엔티티와 유스케이스가 22장의 클린 아키텍처 동심원 구조에서 핵심적인 두 계층을 형성한다
