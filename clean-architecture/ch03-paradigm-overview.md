# Chapter 3: Paradigm Overview (패러다임 개요)

## 핵심 질문

프로그래밍 패러다임이란 무엇이며, 세 가지 패러다임 각각은 프로그래머에게서 무엇을 앗아가는가? 그리고 왜 앞으로도 새로운 패러다임은 등장하지 않을 것인가?

---

## 1. 패러다임의 탄생

소프트웨어 아키텍처는 코드로부터 시작한다. 따라서 아키텍처에 대한 논의도 코드가 최초로 작성된 시점부터 살펴보는 데서 출발해야 한다.

1938년, 앨런 튜링(Alan Turing)은 컴퓨터 프로그래밍이라고 부르는 분야의 토대를 쌓았다. 튜링은 프로그래밍이 가능한 머신을 최초로 상상한 사람은 아니었지만, **프로그램을 단순히 데이터라고 이해한 최초의 사람**이었다. 1945년경에 튜링은 실질적인 프로그램을 실제 컴퓨터에서 코드로 작성했다. 이들 프로그램은 반복문, 분기문, 할당문, 서브루틴, 스택 등 우리에게 익숙한 구조를 사용했다.

이후 프로그래밍에는 수많은 혁신적인 변화가 있었다. 어셈블러(1940년대 후반), 최초의 컴파일러 A0(그레이스 호퍼, 1951년), 포트란(1953년) 등 새로운 프로그래밍 언어가 홍수처럼 쏟아졌다.

그러나 아마도 더 중요한 혁신은 **프로그래밍 패러다임**(paradigm)에서 일어났다. 패러다임이란 프로그래밍을 하는 방법으로, 대체로 언어에는 독립적이다. 패러다임은 어떤 프로그래밍 구조를 사용할지, 그리고 언제 이 구조를 사용해야 하는지를 결정한다. 현재까지 이러한 패러다임에는 **세 가지 종류**가 있다.

---

## 2. 구조적 프로그래밍 (Structured Programming)

최초로 적용된 패러다임(하지만 최초로 만들어진 패러다임은 아닌)은 구조적 프로그래밍으로, **1968년 에츠허르 비버 데이크스트라**(Edsger Wybe Dijkstra)가 발견했다. 데이크스트라는 무분별한 점프(`goto` 문장)가 프로그램 구조에 해롭다는 사실을 제시했다. 이러한 점프들을 `if/then/else`와 `do/while/until` 같은 더 익숙한 제어 구조로 대체했다.

> **구조적 프로그래밍은 제어흐름의 직접적인 전환에 대해 규칙을 부과한다.**

---

## 3. 객체 지향 프로그래밍 (Object-Oriented Programming)

두 번째로 도입된 패러다임은 구조적 프로그래밍보다 사실 2년 앞선 **1966년**, 올레 요한 달(Ole Johan Dahl)과 크리스텐 니가드(Kristen Nygaard)에 의해 등장했다. 이들은 알골(ALGOL) 언어의 함수 호출 스택 프레임을 힙(heap)으로 옮기면, 함수 호출이 반환된 이후에도 함수에서 선언된 지역 변수가 오랫동안 유지될 수 있음을 발견했다. 이 함수가 클래스의 생성자가 되었고, 지역 변수는 인스턴스 변수, 중첩 함수는 메서드가 되었다. 함수 포인터를 특정 규칙에 따라 사용하는 과정을 통해 필연적으로 **다형성**이 등장했다.

> **객체 지향 프로그래밍은 제어흐름의 간접적인 전환에 대해 규칙을 부과한다.**

---

## 4. 함수형 프로그래밍 (Functional Programming)

세 번째 패러다임은 최근에 들어서야 겨우 도입되기 시작했지만, 세 패러다임 중 **가장 먼저 만들어졌다**. 함수형 프로그래밍은 컴퓨터 프로그래밍 자체보다 먼저 등장했다. 알론조 처치(Alonzo Church)가 람다(lambda) 계산법을 발명했고, 1958년에 존 매카시(John McCarthy)가 만든 LISP 언어의 근간이 바로 이 람다 계산법이다. 람다 계산법의 기초가 되는 개념은 **불변성**(immutability)으로, 심볼(symbol)의 값이 변경되지 않는다는 개념이다. 이는 함수형 언어에는 할당문이 전혀 없다는 뜻이기도 하다.

> **함수형 프로그래밍은 할당문에 대해 규칙을 부과한다.**

---

## 5. 생각할 거리: 패러다임은 빼앗는다

세 가지 프로그래밍 패러다임을 소개하면서 주의 깊게 살펴보면 하나의 패턴이 보인다. **각 패러다임은 프로그래머에게서 권한을 박탈한다.** 어느 패러다임도 새로운 권한을 부여하지 않는다. 각 패러다임은 부정적인 의도를 가지는 일종의 추가적인 규칙을 부과한다. 즉, 패러다임은 무엇을 해야 할지를 말하기보다는 **무엇을 해서는 안 되는지를 말해준다.**

| 패러다임 | 등장 연도 | 프로그래머에게서 빼앗은 것 |
|---------|----------|------------------------|
| 구조적 프로그래밍 | 1968년(적용) | `goto`문 (제어흐름의 직접적 전환) |
| 객체 지향 프로그래밍 | 1966년(발견) | 함수 포인터 (제어흐름의 간접적 전환) |
| 함수형 프로그래밍 | 1958년(LISP) | 할당문 (변수 값 변경) |

우리에게서 가져갈 수 있는 게 더 남아 있는가? 아마 없을 것이다. 따라서 **프로그래밍 패러다임은 앞으로도 딱 세 가지밖에 없을 것이다.** 이 밖에 다른 패러다임이 없다는 또 다른 증거로는, 이들 패러다임이 1958년부터 1968년에 걸친 10년 동안 모두 만들어졌다는 사실이다. 이후로 수십 년이 지났지만, 새롭게 등장한 패러다임은 전혀 없다.

> **핵심 통찰**: 패러다임은 프로그래머에게 새로운 능력을 부여하지 않는다. 오히려 무엇을 하지 말아야 하는지를 알려줄 뿐이다. 이 세 가지 외에 빼앗을 것이 더 이상 없으므로, 새로운 패러다임은 등장하지 않을 것이다.

---

## 6. 결론: 패러다임과 아키텍처의 관계

패러다임의 역사로부터 얻을 수 있는 교훈은 아키텍처와 모두 관계가 있다.

| 아키텍처 관심사 | 대응 패러다임 | 역할 |
|---------------|-------------|------|
| 컴포넌트 분리 (경계를 넘나들기) | 객체 지향 프로그래밍 | 다형성을 이용한 아키텍처 경계 메커니즘 |
| 데이터 관리 | 함수형 프로그래밍 | 데이터의 위치와 접근 방법에 대한 규칙 부과 |
| 함수 (모듈의 기반 알고리즘) | 구조적 프로그래밍 | 모듈 내부의 알고리즘 구현 |

세 가지 패러다임과 아키텍처의 세 가지 큰 관심사(함수, 컴포넌트 분리, 데이터 관리)가 어떻게 서로 연관되는지에 주목하자.

---

## 요약

- **프로그래밍 패러다임**은 프로그래밍을 하는 방법이며, 대체로 언어에는 독립적이다.
- **구조적 프로그래밍**(1968)은 제어흐름의 직접적인 전환(`goto`)에 대해 규칙을 부과한다.
- **객체 지향 프로그래밍**(1966)은 제어흐름의 간접적인 전환(함수 포인터)에 대해 규칙을 부과한다.
- **함수형 프로그래밍**(1958, LISP)은 할당문에 대해 규칙을 부과한다.
- 세 패러다임 모두 프로그래머에게서 **권한을 박탈**한다. 새로운 능력을 부여하는 패러다임은 없다.
- 이 세 가지 외에 새로운 패러다임은 등장하지 않을 것이다 — 빼앗을 것이 더 이상 남아 있지 않기 때문이다.
- 세 패러다임은 아키텍처의 세 가지 핵심 관심사(함수, 컴포넌트 분리, 데이터 관리)에 각각 대응한다.

---

## 다른 챕터와의 관계

- **Chapter 4 (구조적 프로그래밍)**: 이 챕터에서 소개한 구조적 프로그래밍의 역사와 원리를 깊이 탐구한다. 데이크스트라의 증명 시도, `goto`문 논쟁, 그리고 과학적 방법과의 관계를 다룬다.
- **Chapter 5 (객체 지향 프로그래밍)**: 캡슐화, 상속, 다형성이라는 OO의 세 가지 주문을 분석하고, OO의 진정한 가치가 의존성 역전에 있음을 밝힌다.
- **Chapter 6 (함수형 프로그래밍)**: 불변성의 개념, 가변성의 분리, 이벤트 소싱 등을 통해 함수형 프로그래밍이 아키텍처에 미치는 영향을 설명한다.
- **Chapter 1 (설계와 아키텍처란?)**: 좋은 아키텍처를 이해하기 위한 첫 걸음으로, 이 챕터에서 다루는 세 가지 패러다임이 그 토대를 형성한다.
- **Chapter 7~11 (SOLID 원칙)**: 패러다임이 제공하는 토대 위에서, SOLID 원칙은 모듈 수준의 설계 원칙을 정의한다.
