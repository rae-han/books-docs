# Appendix A: Performance Testing Basics (처음 해보는 성능 테스트를 위한 기본 정리)

## 핵심 질문

내가 만든 서버가 어느 정도의 트래픽을 감당할 수 있는지 어떻게 확인하는가? 성능 테스트는 어떤 종류가 있고, 테스트를 설계하고 실행할 때 어떤 점을 고려해야 하는가?

---

## 1. 성능 테스트 종류

내가 만든 서버가 어느 정도의 트래픽을 감당할 수 있는지 확인하려면 실제로 측정을 해봐야 한다. 이런 측정은 성능 테스트를 통해 이루어진다. 위키피디아의 정의에 따르면, 성능 테스트는 **특정 작업 부하 상태에서 응답성과 안정성 측면에서 시스템이 어떻게 작동하는지를 확인하기 위해 수행하는 테스트 관행**이다. 응답 시간, 처리량, 자원 사용량(CPU, 메모리 등)을 주로 측정한다.

주로 사용하는 성능 테스트의 종류는 다음과 같다.

| 테스트 종류 | 설명 |
|------------|------|
| **부하(Load) 테스트** | 특정한 예상 부하에서 시스템이 어떻게 동작하는지 확인한다. 이 테스트를 통해 주요 기능의 응답 시간과 처리량 등의 성능 지표를 확인할 수 있고, 병목을 파악하는 데 도움이 된다 |
| **스트레스(Stress) 테스트** | 시스템의 최대 성능을 확인하기 위한 테스트이다. 예상을 뛰어넘는 부하가 발생했을 때 시스템이 어디까지 성능을 낼 수 있는지를 확인한다 |
| **지속 부하(Soak) 테스트** | 시스템이 지속적인 부하를 견딜 수 있는지를 검증한다. 장시간 동안 일정 수준의 부하를 주어 성능 저하가 발생하는지 확인하며 메모리 누수도 탐지할 수 있다 |
| **스파이크(Spike) 테스트** | 급격하게 트래픽이 변화할 때 시스템의 반응성과 안정성을 검증하는 테스트이다. 순간적으로 트래픽이 급증했을 때 성능 저하나 실패가 발생하는지를 확인한다 |

> **핵심 통찰**: 네 가지 테스트는 각각 다른 질문에 답한다. 부하 테스트는 "평소 트래픽을 잘 처리하는가?", 스트레스 테스트는 "최대 어디까지 버티는가?", 지속 부하 테스트는 "오래 돌려도 안정적인가?", 스파이크 테스트는 "갑자기 트래픽이 몰려도 괜찮은가?"에 대한 답을 제공한다.

---

## 2. 포화점과 버클존

성능 테스트를 진행하는 일반적인 방식은 **낮은 부하에서 시작해서 점진적으로 부하를 높이는 것**이다. 예를 들어, 동시 사용자 수를 100명에서 시작해 200명, 300명으로 단계적으로 부하를 높여가며 성능을 측정한다.

이렇게 부하를 증가시키면 초기에는 처리량도 함께 증가한다. 그러다 일정 부하 구간에 도달하면 처리량의 증가 폭이 줄어들기 시작하고, 어느 시점부터는 처리량과 응답 시간이 급격히 저하된다.

### 포화점(Saturation Point)

포화점(*Saturation Point - 성능이 저하되기 직전의 최대 처리량 지점*)은 시스템이 감당할 수 있는 성능 한계 지점이다. 부하를 점진적으로 늘릴 때, 처리량이 더 이상 증가하지 않고 꺾이기 시작하는 바로 그 지점을 말한다.

### 버클존(Buckle Zone)

포화점을 지나 성능이 꺾이기 시작하는 구간을 버클존(*Buckle Zone*)이라고 한다.

### 알아두기: 버클(Buckle)

버클링(*Buckling - 좌굴*)은 물리 현상에서 빌려온 개념으로, 세로로 긴 구조물을 세로 방향으로 압축할 때 어느 순간 압축 하중을 견디지 못하고 구부러지는 것을 말한다. 버클존의 버클은 이 현상에서 빌려온 단어로, 시스템이 한계를 초과했을 때 급격하게 성능이 저하되는 상황을 표현한다.

```
TPS
 ▲
 │              ┌── 포화점
 │             ╱│
600│           ╱  │
500│         ╱   │
400│       ╱     │  ← 버클존
300│     ╱       │    (급격한 성능 저하)
200│   ╱          ╲
100│ ╱              ╲
 │╱                 ╲
 └──────────────────────▶ 동시 사용자 수
   100 200 300 400 500 600 700 800 900 1000
```

### 포화점 이후의 대응

포화점이 목표로 한 성능보다 높다면 만족하면서 성능 테스트를 마칠 수 있다. 하지만 포화점이 목표치보다 낮거나 같다면 **병목 지점을 찾아 제거해야 한다**. 일반적으로 웹 서버의 병목 지점은 호출 비중이 높으면서도 응답 시간이 긴 기능과 관련되어 있다. 2장에서 살펴본 바와 같이 다음 요인들이 응답 시간에 영향을 준다.

- **DB 연동**: 쿼리 실행 시간
- **외부 연동 시간**: 다른 서비스와의 API 통신
- **트래픽 대비 부족한 커넥션 풀 크기**

어떤 지점에서 병목이 발생하는지 확인한 뒤, 서버 확장, 캐시 적용, 비동기 연동과 같은 수단을 강구해서 응답 시간을 줄여보자. 응답 시간을 줄인 만큼 포화점을 높일 수 있을 것이다.

> **실무 팁**: 성능 테스트 결과에서 포화점과 버클존을 명확히 식별하는 것이 핵심이다. 포화점을 기준으로 시스템의 현재 용량을 파악하고, 버클존에 진입하지 않도록 충분한 여유를 두고 용량을 계획해야 한다.

---

## 3. 주요 측정 지표

### 3.1 응답 시간

성능 테스트에서 중요한 지표 중 하나는 응답 시간이다. 응답 시간은 다음과 같이 여러 값을 측정한다.

| 지표 | 설명 |
|------|------|
| **평균** | 전체 응답 시간의 산술 평균 |
| **최대** | 가장 오래 걸린 응답 시간 |
| **최소** | 가장 짧은 응답 시간 |
| **중앙값** | 전체 응답 시간을 정렬했을 때 가운데 위치한 값 |
| **99%/95% 백분위** | 전체 응답의 99% 또는 95%가 이 시간 이내에 처리됨 |

응답 시간의 평균과 최대값의 차이가 크다면 99%나 95% 백분위 값을 함께 확인해야 한다. 이 백분위 값이 최대 응답 시간보다 평균값에 가깝다면 최대값은 이상치일 수 있다.

중앙값과 평균값의 차이도 중요하다. 응답 시간이 중앙값보다 평균값이 더 큰 좌편향 분포를 보인다면, 이는 응답 시간이 전반적으로 평균보다 느리다는 것을 의미한다.

> **핵심 통찰**: 평균 응답 시간만으로 성능을 판단하면 안 된다. 평균은 이상치에 크게 영향을 받기 때문이다. **p99(99% 백분위)나 p95(95% 백분위)** 를 함께 확인해야 실제 사용자 경험에 가까운 성능을 파악할 수 있다.

### 3.2 처리량

처리량은 TPS(*Transaction Per Second - 초당 트랜잭션 건수*)처럼 초 단위로 얼마나 많은 요청을 처리했는지를 나타낸다. 테스트를 진행하는 동안 처리량은 변화하므로 **최대, 평균, 최소값**을 함께 구한다.

### 3.3 에러율

에러율은 전체 요청 중에서 에러가 발생한 비율을 의미한다. 부하 테스트를 하다 보면, 처리량은 높은데 에러도 함께 증가하는 상황이 생긴다. 에러가 발생한다는 건 시스템이 부하를 감당하지 못하고 있다는 신호일 수 있다. **에러가 증가하기 시작하는 지점이 시스템의 한계일 가능성이 높다.**

### 3.4 CPU 사용률

부하 테스트 중에는 대상 시스템의 CPU 사용률도 함께 모니터링해야 한다. 일반적으로 웹 서버의 성능 문제는 DB나 외부 연동에서 발생하지만, 높은 CPU 사용률로 인해 문제가 생기는 경우도 있다. CPU 사용률을 측정하면 문제 원인을 분석할 때 범위를 좁히는 데 도움이 된다.

---

## 4. 성능 테스트 설계 시 고려사항

성능 테스트를 설계할 때는 다음 사항들을 고려해야 한다.

### 4.1 시스템의 트래픽 패턴

시스템은 보통 일정한 트래픽 패턴을 가진다. 일부 서비스는 시간에 따라 트래픽 편차가 크지 않지만, 일부는 특정 시간대에 트래픽이 집중되기도 한다.

| 서비스 유형 | 트래픽 패턴 | 테스트 설계 방식 |
|------------|-----------|----------------|
| **콜센터 업무 시스템** | 업무 시작~종료까지 비교적 고른 분포 | 목표 부하를 바로 발생시키는 방식 |
| **학원 관련 서비스** | 주중 오후 시간대에 급증 | 짧은 시간 간격으로 부하를 늘리는 방식 |

트래픽이 짧은 시간에 증가하는 패턴을 보인다면 그에 맞춰 성능 테스트도 짧은 시간 간격으로 부하를 늘리는 방식으로 설계한다. 업무 시스템은 부하가 점진적으로 증가하기보다는 특정 시간에 목표로 하는 부하가 발생하므로, 해당 부하를 바로 발생하도록 성능 테스트를 설계한다.

### 4.2 동시 요청 사용자 수 / 트래픽 규모

동시에 사용하는 사용자 규모도 고려한다. 50명이 사용하는 내부 시스템과 1천만이 사용하는 온라인 서비스는 처리해야 하는 트래픽 규모가 다르다. 트래픽 규모가 작으면 한 대의 부하 발생기로도 충분하지만, 트래픽 규모가 큰 서비스는 여러 대의 부하 발생기를 사용해야 할 수 있다.

부하에 사용할 사용자 수도 중요하다. 예를 들어 **한 사용자의 인증 토큰을 사용해서 모든 요청을 생성한다면** 시스템은 실제보다 더 좋은 성능을 낼 수 있다. DB가 자주 조회하는 데이터를 캐싱해서 조회 속도가 빨라질 수 있기 때문이다. 사용자에 따라 조회하는 데이터가 다르다면, 목표로 하는 동시 사용자 수에 맞춰 요청을 발생시킬 수 있도록 테스트를 설계해야 한다.

### 4.3 기능별 요청 비율

부하 테스트를 설계할 때는 기능별 트래픽 비율도 고려해야 한다. 자주 불리는 API 목록을 추리고 **최대한 실제 호출되는 비율에 맞게** 부하가 발생하도록 테스트를 설계한다.

실제 불리는 비율을 고려하지 않고 1개 API에 대해서만 부하를 발생하면, 캐시 효과와 같은 이유로 결과가 실제보다 잘 나올 수 있다.

### 4.4 데이터 크기

데이터 크기도 고려한다. 데이터가 100만 건이 예상되는 서비스에 대해 부하 테스트를 하는데 DB에 데이터를 50건만 넣으면 많은 경우 의미가 없다. 데이터 규모가 작으면 성능이 잘 나오기 때문이다.

반대로, 데이터가 많아야 1년에 2천 개밖에 생성되지 않는 기능에 대해 스트레스 테스트 목적으로 50만 개의 데이터를 만들면 안 된다. **예상되는 규모에 맞게 테스트 데이터를 만들어야** 실제에 근접한 성능 테스트 결과를 얻을 수 있다.

### 4.5 워밍업

테스트를 진행하기에 앞서 워밍업(*Warm-up - 시스템이 안정 상태에 도달하도록 사전에 준비하는 과정*)이 필요할 때도 있다. 레디스를 캐시로 사용하는 서비스에 대해 부하 테스트를 진행하는 경우를 생각해 보자. 최초에 캐시에 아무 데이터도 존재하지 않으면 초기 요청은 캐시에 데이터가 없으므로 전반적으로 성능(처리량과 응답 시간)이 떨어질 것이다. 반면에 캐시에 어느 정도 데이터가 차기 시작하면 성능도 함께 좋아진다.

부하 테스트의 목적이 트래픽이 몰리는 상황에서 캐시를 비울 때 어느 정도 성능이 나오는지 확인하기 위한 것이 아니라면, **캐시가 어느 정도 찬 다음에** 부하 테스트를 진행해야 올바른 결과를 얻을 수 있다.

워밍업 방식의 예시:

- 테스트 진행 전에 캐시에 데이터를 어느 정도 채워넣기
- 점진적으로 트래픽을 늘리기: 첫 30초 동안은 20명의 동시 사용자로 부하를 발생하고, 이후 15초마다 동시 사용자를 20명씩 늘리는 방식

### 4.6 적절한 목표치 설정

적절한 목표치도 중요하다. 최고의 성능을 내는 서버를 만들겠다는 의지를 담아 처리량 목표치를 10만 TPS로 잡을 수 있지만, 이는 비현실적인 수치다. 10만 TPS는 10만 명의 사용자가 1초마다 계속 클릭해야 발생할 수 있는 트래픽 규모다. **실제 예상되는 사용자 수와 요청 건수를 감안해서** 성능 목표치를 설정하자.

> **실무 팁**: 성능 테스트 설계에서 가장 중요한 원칙은 "실제 운영 환경을 최대한 재현하는 것"이다. 트래픽 패턴, 사용자 수, API 호출 비율, 데이터 규모 등 모든 요소가 실제와 가까울수록 신뢰할 수 있는 결과를 얻을 수 있다.

---

## 5. 성능 테스트 도구

### 5.1 nGrinder

nGrinder는 네이버에서 개발한 부하 테스트 도구로, grinder를 기반으로 하되 사용이 더 편리한 버전이다.

**구조:**

```
                    ┌──────────┐
                    │ 컨트롤러   │ ← 웹 UI 제공, 테스트 작업(스크립트) 관리
                    │          │
                    └────┬─────┘
              ┌──────────┼──────────┐
              ▼          ▼          ▼
        ┌──────────┐ ┌──────────┐
        │ 에이전트   │ │ 에이전트   │ ← 실제 부하 발생 + 시스템 지표(CPU, 메모리) 모니터링
        └────┬─────┘ └────┬─────┘
             │ 부하        │ 부하
             ▼             ▼
        ┌─────────────────────┐
        │   테스트 대상 시스템    │
        └─────────────────────┘
```

- 1개의 **컨트롤러**와 다수의 **에이전트**로 구성
- 컨트롤러는 웹 UI를 제공하여 사용이 쉬움
- 에이전트는 실제로 부하를 발생하고 테스트 대상 서버의 시스템 지표(CPU와 메모리)를 모니터링
- 2개 이상의 에이전트를 사용할 수 있기 때문에 여러 장비를 활용해서 대량의 부하를 발생시킬 수 있음
- **Groovy**나 **Jython**을 이용해서 테스트용 스크립트를 작성

### 5.2 k6

k6는 Grafana Labs에서 개발한 부하 테스트 도구이다. 비교적 최근에 나온 도구로 설치가 쉽다.

- Go 언어로 개발되었지만 테스트 스크립트는 **JavaScript**를 이용해서 작성
- **고루틴**을 사용해서 부하를 발생시키기 때문에 스레드를 사용하는 도구 대비 더 적은 자원으로 더 많은 부하를 발생할 수 있음
- **CLI로 실행**하므로 CI/CD 환경에 통합이 용이
- 부하 테스트 결과도 콘솔에 출력
- Prometheus나 InfluxDB 같은 곳에 결과를 저장해서 실시간 테스트 상황을 모니터링할 수 있음
- 별도의 리포트 플러그인을 사용해서 HTML과 같은 형식으로 결과를 생성 가능
- 동시에 여러 장비에서 부하를 발생시키려면 유료 버전인 k6 클라우드를 사용하거나, 쿠버네티스 클러스터가 있다면 k6 Operator를 사용해서 분산 환경에서 부하 발생 가능

### 5.3 기타 도구

| 도구 | 스크립트 언어 | 특징 |
|------|-------------|------|
| **Locust** | Python | 명령행과 웹 기반 UI로 테스트 실행 가능. 분산 환경에서 부하 발생 기능 제공. 클라우드 버전도 제공 |
| **Gatling** | Java, Scala, Kotlin, JavaScript | 웹 요청 외에 AMQP나 카프카 등 다양한 프로토콜 지원. 엔터프라이즈 버전으로 분산 환경 지원 |
| **JMeter** | GUI/CLI (XML 기반 설정) | 오랜 기간 유지되어 온 부하 테스트 도구. FTP, LDAP, TCP 등 다양한 프로토콜 지원. 분산 환경 지원 |

> **실무 팁**: 어떤 도구가 더 좋다기보다는 각자 상황에 맞춰 도구를 선택하면 된다. JVM 환경이 익숙하다면 nGrinder나 Gatling을 사용하고, JavaScript가 익숙하다면 k6를 시도해 보자. Python이 주 언어라면 Locust도 좋은 선택이다.

---

## 6. 성능 테스트 실행 시 주의사항

### 6.1 부하기와 테스트 대상 시스템을 분리하라

성능 테스트를 진행할 때 흔히 하는 실수 중 하나가 **테스트 대상 시스템과 부하기를 한 장비에서 실행하는 것**이다.

```
[잘못된 구성]                        [올바른 구성]
┌─────────────────────┐            ┌──────────┐     ┌──────────────┐
│     동일 서버         │            │  부하기   │────▶│ 테스트 대상    │
│  ┌───────┐ ┌──────┐ │            │  (장비 A) │     │ 시스템(장비 B) │
│  │ 부하기 │ │ 대상  │ │            └──────────┘     └──────────────┘
│  └───────┘ └──────┘ │
└─────────────────────┘
```

부하 생성은 그 자체로 많은 자원을 사용한다. 따라서 테스트 대상 시스템과 부하기를 한 장비에서 실행하면 테스트 대상 시스템이 자원을 온전히 활용하지 못해 **실제 성능보다 낮은 결과**가 나온다.

성능 테스트를 진행할 때는 반드시 부하 발생기와 테스트 대상 시스템을 분리해야 한다. 부하를 발생할 장비의 성능도 중요하다. 대량의 부하를 발생시켜야 하는데 장비의 성능이 나쁘면 애초에 부하 자체를 제대로 발생시킬 수 없다.

### 6.2 서버 설정의 제한을 확인하라

서버 설정에 제한을 걸고 부하 테스트를 실행하는 것도 자주 하는 실수다.

| 주의 대상 | 설명 |
|----------|------|
| **요청 수 제한** | Nginx의 `limit_req_zone` 등으로 IP당 초당 요청 개수를 제한한 경우, 부하기는 1대 장비에서 다량의 요청을 발생하므로 제한에 걸려 많은 요청에 오류 응답이 발생한다 |
| **스레드 풀 크기** | 서버의 스레드 풀 개수가 작으면 부하 테스트가 제대로 이루어지지 않는다 |
| **DB 커넥션 풀 크기** | 커넥션 풀의 크기가 작으면 병목이 발생한다 |

테스트할 부하 규모에 맞게 풀의 크기를 미리 설정해야 한다.

### 6.3 운영 네트워크와 분리하라

운영 시스템과 동일한 네트워크 환경에서 부하 테스트를 진행하는 것도 조심해야 한다. 스트레스 테스트처럼 대량의 요청을 발생시킬 때는 테스트 대상 시스템에 많은 네트워크 트래픽이 발생한다. 실 운영 시스템과 테스트 대상 시스템을 동일 네트워크에 넣고 테스트를 실행하면, 테스트가 발생시키는 트래픽이 실 운영 환경에 영향을 줄 수 있다.

예를 들어 스트레스 테스트로 인해 네트워크가 포화 상태에 다다르면 **실제 서비스가 먹통되는 증상**이 발생할 수 있다.

> 대량 트래픽이 예상되는 서비스 개발에 참여한 적이 있다. 개발 초기에 생각한 아키텍처가 원하는 성능을 보여주는지 확인할 필요가 있었고, 이를 위해 부하 테스트 담당자가 개발에 참여했다. 부하 테스트 담당자는 테스트 스크립트를 작성했지만 업무 시간에 테스트를 수행할 수는 없었다. 부하 테스트 환경과 실제 운영 환경이 같은 네트워크에 있었기 때문이다. 부하 테스트 담당자는 실 서비스에 대한 영향을 최소화하기 위해 사용자가 적은 새벽 시간대에 부하 테스트를 진행해야 했다.

### 6.4 외부 서비스를 가짜로 대체하라

외부 서비스를 연동하는 기능을 테스트할 때는 **실제 외부 서비스를 사용하지 않게** 주의해야 한다. 테스트하고 싶은 대상은 우리 시스템이지 외부 시스템이 아니기 때문이다.

외부 시스템 연동을 그대로 둔 채로 부하 테스트를 진행하면 외부 시스템이 트래픽을 견디지 못하고 장애가 발생할 수 있다.

```
[잘못된 구성]
부하기 ──▶ 테스트 대상 시스템 ──▶ 외부 시스템 (장애 위험!)

[올바른 구성]
부하기 ──▶ 테스트 대상 시스템 ──▶ 가짜 외부 서비스 (Mock/Stub)
```

외부 서비스 연동이 포함된 기능에 대해 부하 테스트를 진행해야 한다면, 실제 외부 서비스 대신에 **가짜 외부 서비스(Mock)로 대체**해야 한다.

### 6.5 실 운영 환경과 동일하게 구성하라

테스트 대상 시스템은 최대한 실제 운영 환경과 동일하게 구성해야 한다. 간혹 실제 운영 환경 대비 저사양 환경을 사용해서 테스트 대상을 구성할 때가 있다.

| 구분 | 실 운영 서버 | 테스트 서버 |
|------|------------|-----------|
| CPU | 8코어 | 2코어 |
| 메모리 | 16GB | 2GB |

이렇게 되면 실제 환경에서 얼마나 성능을 낼 수 있는지 확인할 수 없다. **유효한 성능 지표를 구하려면 최대한 실 환경에 맞춰야** 한다.

> 예전에 서비스 점검 시간에 맞춰 부하 테스트를 진행한 적이 있다. 점검 때는 사용자 트래픽이 발생하지 않아 부하 테스트를 하기에 안성맞춤이었다. 점검은 0시부터 진행했고 부하 테스트는 1시 정도부터 시작했다. 점진적으로 부하 수준을 높여가며 테스트를 진행했다. 한 번 실행할 때 5분간 부하를 지속했고 테스트가 종료되면 결과를 기록했다. 이렇게 여러 차례 테스트를 실행했고 이 과정에서 일부 기능에 성능 문제가 발생한 것을 확인했다. 문제를 분석하고, 원인을 제거한 뒤 다시 테스트 과정을 반복했다. 목표로 하는 성능이 나오는 것을 확인하기까지 1시간 정도의 시간이 소요됐다.

> **핵심 통찰**: 성능 테스트는 지루한 과정의 연속이다. 목표로 한 성능이 안 나오면 병목을 찾기 위해 시스템을 분석해야 한다. 의심되는 코드를 찾아 수정했다면 다시 성능 테스트를 진행한다. 원하는 결과가 나올 때까지 이 과정을 반복한다. 이러한 반복 과정이야말로 성능 테스트의 본질이다.

---

## 자주 하는 실수

| 안티패턴 | 문제점 | 해결법 |
|---------|--------|--------|
| 부하기와 테스트 대상 시스템을 한 장비에서 실행 | 자원 경합으로 실제보다 낮은 성능 결과 | 부하 발생기와 테스트 대상 시스템을 반드시 분리 |
| 서버의 요청 제한 설정을 해제하지 않고 테스트 | 제한에 걸려 대부분의 요청에 오류 응답 발생 | DDoS 방어 설정, 스레드 풀, 커넥션 풀 크기 등을 부하 규모에 맞게 조정 |
| 실 운영과 동일 네트워크에서 스트레스 테스트 실행 | 테스트 트래픽이 실 운영 서비스에 영향 → 장애 | 테스트 환경을 운영 네트워크와 분리, 불가피하면 심야 시간대 활용 |
| 실제 외부 서비스를 연동한 채로 부하 테스트 | 외부 시스템에 장애 유발 | 가짜 외부 서비스(Mock/Stub)로 대체 |
| 한 사용자의 인증 토큰으로 모든 요청 생성 | DB 캐싱 효과로 실제보다 좋은 결과 | 목표 동시 사용자 수에 맞춰 다양한 사용자 토큰 사용 |
| 실 운영 대비 저사양 환경에서 테스트 | 실 환경 성능을 예측할 수 없음 | 최대한 실 운영 환경과 동일한 사양으로 구성 |
| 데이터 규모를 고려하지 않은 테스트 | 데이터가 적으면 성능이 과대평가됨 | 예상되는 데이터 규모에 맞게 테스트 데이터 준비 |
| 워밍업 없이 바로 본 테스트 진행 | 캐시 미적중으로 초반 성능이 과소평가됨 | 사전에 캐시 데이터를 채우거나 점진적으로 트래픽 증가 |

---

## 요약

- 성능 테스트는 특정 작업 부하 상태에서 시스템의 응답성과 안정성을 확인하기 위해 수행하며, **부하 테스트**, **스트레스 테스트**, **지속 부하 테스트**, **스파이크 테스트** 4가지 종류가 있다
- 부하를 점진적으로 높일 때 처리량이 더 이상 증가하지 않는 지점을 **포화점**이라 하고, 포화점을 지나 성능이 급격히 저하되는 구간을 **버클존**이라 한다. 포화점이 목표치보다 낮으면 병목을 찾아 제거해야 한다
- 주요 측정 지표는 **응답 시간**(평균, 최대, 중앙, 백분위), **처리량**(TPS), **에러율**, **CPU 사용률**이다. 평균만으로 판단하지 말고 p99/p95 백분위를 함께 확인해야 한다
- 성능 테스트 설계 시 **트래픽 패턴**, **동시 사용자 수**, **기능별 요청 비율**, **데이터 크기**, **워밍업**, **적절한 목표치**를 고려해야 한다. 핵심 원칙은 실제 운영 환경을 최대한 재현하는 것이다
- 대표적인 성능 테스트 도구로는 **nGrinder**(네이버, Groovy/Jython), **k6**(Grafana Labs, JavaScript), **Locust**(Python), **Gatling**(JVM), **JMeter**(다양한 프로토콜)가 있다
- 성능 테스트 실행 시 **부하기와 테스트 대상을 분리**하고, **서버 설정 제한을 확인**하고, **운영 네트워크와 분리**하고, **외부 서비스를 가짜로 대체**하고, **실 운영 환경과 동일한 사양**으로 구성해야 유효한 결과를 얻을 수 있다
